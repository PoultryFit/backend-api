# Backend 3 — Disease / ML API

Ownership: the entire `disease-api/` service.

## Exact source preservation
The files at the root of this repository are a byte-for-byte copy of the uploaded project's `disease-api/` directory. No source/model/config files were rewritten.

This member sends the disease API files/service. The TypeScript application talks to it through `ML_SERVICE_URL` and optional `ML_SERVICE_API_KEY`.
